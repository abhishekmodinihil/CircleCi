#
# Build configuration for Circle CI
#
version 2

jobs:
    build:
      working_directory: ~/code

      docker:
       - image: circleci/android:api-26-alpha

      enviornment:
        JVM_OPTS: -Xmx3200m

      steps:
      - checkout